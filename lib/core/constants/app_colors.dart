import 'package:flutter/material.dart';

class AppColors {
  // ─── Primary (Dark Orange / Burnt Orange) ───
  static const Color primary = Color(0xFFE65100);        // Deep Orange-900
  static const Color primaryLight = Color(0xFFFF6D00);    // Orange-A700
  static const Color primaryDark = Color(0xFFBF360C);     // Deep Orange-900 darker
  static const Color primaryDeep = Color(0xFF8B2500);     // Very deep burnt
  static const Color primarySurface = Color(0xFFFFF3E0);  // Orange-50
  static const Color primaryMuted = Color(0xFFFFE0B2);    // Orange-100

  // ─── Credit / Debit ───
  static const Color credit = Color(0xFF059669);    // Emerald-600 (money in)
  static const Color debit = Color(0xFFE11D48);     // Rose-600 (money out)
  static const Color creditBg = Color(0xFFECFDF5);  // Emerald-50
  static const Color debitBg = Color(0xFFFFF1F2);   // Rose-50

  // ─── Surfaces & Backgrounds ───
  static const Color background = Color(0xFFFAFAFA);   // Neutral-50
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE8E0D8);      // Warm gray
  static const Color textPrimary = Color(0xFF1C1410);   // Dark warm black
  static const Color textSecondary = Color(0xFF78716C); // Stone-500
  static const Color textHint = Color(0xFFA8A29E);      // Stone-400
  static const Color iconColor = Color(0xFF57534E);     // Stone-600

  // ─── Accent ───
  static const Color amber = Color(0xFFF59E0B);    // Amber-500
  static const Color amberBg = Color(0xFFFFFBEB);  // Amber-50
  static const Color indigo = Color(0xFF6366F1);   // Indigo-500
  static const Color indigoBg = Color(0xFFEEF2FF); // Indigo-50

  // ─── Misc ───
  static const Color shadow = Color(0x14000000);    // Subtle shadow
  static const Color shimmer = Color(0xFFF5F0EB);  // Warm shimmer bg
}
